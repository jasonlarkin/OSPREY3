# Open Babel Adapters for next Interfaces

Wraps Open Babel's existing components as next generalized interfaces.

## Adapters

- **OBForceFieldTerm**: `OBForceField` → `IEnergyTerm`
- **OBConformerNeighborhood**: `OBConformerSearch` → `INeighborhood`
