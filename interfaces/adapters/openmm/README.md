# OpenMM Adapters for next Interfaces

Wraps OpenMM's existing components as next generalized interfaces.

## Adapters

- **ForceTerm**: `Force` → `IEnergyTerm`
- **SystemWrapper**: `System` → `ISystem`
- **ContextState**: `Context` → `IState`
